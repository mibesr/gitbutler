{
	"productName": "GitButler Test",
	"identifier": "com.gitbutler.app.test",
	"build": {
		"beforeBuildCommand": "[ $CI = true ] || pnpm build:desktop -- --mode development && cargo build -p gitbutler-git && bash ./crates/gitbutler-tauri/inject-git-binaries.sh debug",
		"frontendDist": "../../apps/desktop/build"
	},
	"bundle": {
		"active": false
	}
}
